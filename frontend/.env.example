# Frontend Environment Variables

# NOTE: This project currently does not require any environment variables.
# The frontend connects directly to the deployed backend API.

# Backend API is hardcoded in: src/config/api.js
# Backend URL: https://cyphers101.onrender.com/api/chat

# Future environment variables (not currently used):
# VITE_API_BASE_URL=http://localhost:8000/api
# VITE_WS_URL=ws://localhost:8000/signal
# VITE_DEFAULT_THEME=light
